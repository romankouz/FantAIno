[project]
name = "fantaino"
description = "A simple package structure for the FantAIno project."
version = "0.1.0"
authors = [
    { name = "Roman Kouznetsov", email = "roko@umich.edu" }
]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["FantAIno*"]


[project.optional-dependencies]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.pylint.main]
# Paths are relative to this file; helps Pylint resolve `import FantAIno` from scripts under e.g. data/scripts/
pythonpath = ["."]